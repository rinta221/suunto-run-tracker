# suunto-run-tracker
Suunto連携ランニング練習記録アプリ（Node.js + SQLite + Claude/ChatPT AI評価）

## 起動・停止

### サーバー起動
```bash
npm start
```
サーバーが http://localhost:3000 で起動します。

### サーバー停止
`Ctrl + C` でサーバーを停止できます。

### 開発モード（自動再起動）
```bash
npm run dev
```
ファイル変更時に自動でサーバーが再起動します。
